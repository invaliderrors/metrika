{
  "name": "@metrika/workers",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "uv run --locked --all-packages ruff check .",
    "format": "uv run --locked --all-packages ruff format .",
    "format:check": "uv run --locked --all-packages ruff format --check .",
    "typecheck": "uv run --locked --all-packages mypy .",
    "test:unit": "uv run --locked --all-packages pytest"
  }
}
